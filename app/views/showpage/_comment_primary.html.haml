.columns.large-6.comment_primary
  .panel
    .row
      .columns.large-12
        %h3
          %span.first
            = @taiketsu.topics.first.topic
          からのご意見

    - @comments.each_with_index do |comment, i|
      - if i % 2 == 0
        .comment_block.bgc-gray
          .row
            .columns.large-12
              %p
                = comment.text
      - else
        .comment_block.bgc-white
          .row
            .columns.large-12
              %p
                = comment.text
      .row
        .colums.large-12
    = form_for [@taiketsu,@comment] do |f|
      .form
        = f.text_field :text, placeholder: '意見を入力してください', value: "", class: 'new_comment'
        = f.hidden_field :topic_id, value: @taiketsu.topics.first.id
        .form__submit
        = f.submit value: '投稿する', class: 'submit'
